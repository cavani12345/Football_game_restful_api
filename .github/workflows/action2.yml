name: Deploy Application to GAE
on: 
  pull_request:
    types:
      - closed
jobs:
  if_merged:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
      - name: checking
        run: echo " this for checking purposes"

 